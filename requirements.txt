# =============================================================================
# AI Camera System – Backend Python Dependencies
# Pin major versions; allow patch updates.
# =============================================================================

# --- Web Framework -----------------------------------------------------------
fastapi==0.111.*
uvicorn[standard]==0.30.*          # includes uvloop, httptools
python-multipart==0.0.*            # multipart/form-data for image uploads

# --- Async HTTP --------------------------------------------------------------
httpx==0.27.*                      # Async HTTP client (Odoo webhook calls)

# --- Task Queue --------------------------------------------------------------
celery[redis]==5.4.*
redis==5.0.*                       # Python Redis client (broker + pub/sub)

# --- Database (Odoo PostgreSQL + pgvector) -----------------------------------
sqlalchemy[asyncio]==2.0.*
asyncpg==0.29.*                    # Async PostgreSQL driver
psycopg2-binary==2.9.*             # Sync driver (Alembic / scripts)
pgvector==0.3.*                    # pgvector SQLAlchemy / psycopg2 type support

# --- AI / Computer Vision ----------------------------------------------------
insightface==0.7.*
onnxruntime==1.18.*                # CPU-only; do NOT install onnxruntime-gpu
numpy==1.26.*
opencv-python-headless==4.10.*     # headless: no GUI deps (libGL still needed)
Pillow==10.*

# --- Configuration & Validation ----------------------------------------------
pydantic==2.7.*
pydantic-settings==2.3.*           # BaseSettings with .env file loading

# --- Utilities ---------------------------------------------------------------
python-dotenv==1.0.*
structlog==24.*                    # Structured JSON logging
tenacity==8.*                      # Retry logic for DB / Odoo HTTP calls